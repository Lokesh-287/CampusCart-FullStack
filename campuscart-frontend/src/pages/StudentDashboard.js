import React from 'react';
import ProductList from '../components/ProductList'; // Import the new component

const StudentDashboard = () => {
    return (
        <div>
            <h1>Depot Products</h1>
            <ProductList />
        </div>
    );
};

export default StudentDashboard;